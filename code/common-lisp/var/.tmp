(defvar k)

(defun make-fibonacci-generator ()
  (let ((previous 1)
		(current 0))
	(function (lambda ()
				(prog1 current
				  (let ((temp current))
					(setf current (+ current previous))
					(setf previous temp)
					)
				  )
				))
	)
  )

(setf k (make-fibonacci-generator))
